{
  "name": "carver",
  "version": "1.0.0",
  "description": "Seam carving web app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run build:wasm && vite build",
    "build:wasm": "cargo build --target wasm32-unknown-unknown --release -p carver-wasm && wasm-bindgen crates/carver-wasm/target/wasm32-unknown-unknown/release/carver_wasm.wasm --out-dir src/wasm/pkg --target web --typescript",
    "test": "vitest",
    "test:rust": "cargo test -p carver-wasm",
    "lint": "oxlint src",
    "fmt": "oxfmt src",
    "fmt:check": "oxfmt --check src"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "jsdom": "^24.0.0",
    "typescript": "^5.0.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0",
    "oxlint": "latest",
    "oxfmt": "latest"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "author": "Andrew Jesaitis",
  "license": "MIT"
}
